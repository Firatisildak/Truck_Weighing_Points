@page "/deleteproduct/{id}"
@using BlazorWASM_GeotekProject.Models
@inject HttpClient Http
@inject NavigationManager NavigationManager

<h3>Delete Product</h3>
<p>Are you sure you want to delete this Product with Id: <b>@id</b>?</p>
<br />
<div class="col-md-4">
    <table class="table table-bordered">
        <tr>
            <td>ID</td>
            <td>@product.ProdId</td>
        </tr>
        <tr>
            <td>Name</td>
            <td>@product.ProdName</td>
        </tr>
        <tr>
            <td>Price</td>
            <td>@product.Price</td>
        </tr>
    </table>
    <div class="form-group">
        <input type="button" value="Delete" @onclick="DeleteProductInfo" class="btn btn-danger" />
        <input type="button" value="Cancel" @onclick="Cancel" class="btn btn-info" />
    </div>
</div>

@code {
    [Parameter]
    public string id { get; set; }
    private Product product = new Product();

    protected override async Task OnInitializedAsync()
    {
        // Ürünü getirmek için doğru URL formatını kullanın.
        product = await Http.GetFromJsonAsync<Product>($"https://localhost:44356/api/Products/GetProduct/{id}");
    }

    private async Task DeleteProductInfo()
    {
        // Ürün silme işlemi için DELETE iste
